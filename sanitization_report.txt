Sanitization scan report (2026-04-01)

Findings:
- Internal host paths found in README.md and ai_client.py: /haconfig/... (these will be neutralized in release docs)
- No plaintext API tokens (gho_, AKIA, etc.) found inside custom_components/ai_varme_styring files.
- Private keys exist elsewhere in repo (.cloud/*.pem) and must NOT be included in release.

Action taken:
- Created clean release folder at _archive/ai-varme-clean-20260401
- Added root and integration `hacs.json` with `content_in_root: false`.
- Added a sanitised top-level `README.md` advising users about host-specific runtime files.

Recommended next steps before pushing to GitHub:
1) Decide whether to include full source files in the clean release. If yes, I will copy them but replace `/haconfig` with `/openclaw-data` and remove any local absolute paths from runtime links.
2) Ensure `secrets.yaml`, private PEM files and `.storage/` are never copied into the release.
3) Optionally run a final grep across the files to be published to confirm no sensitive strings remain.
